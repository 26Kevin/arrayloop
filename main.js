// Create array - Using Loops

// Main Algorith:
// Start with an empty array
// Loop - inside of the loop we add elements to the array

let happyArray = [];
for (let n = 1; n <= 100; n++) {
    happyArray.push(randomDec(5, 8)); 
}
